// Custom Init - Happens only once when mounting the component
sap.ui.getCore().attachInit(function (startParams) {
   modelPanelSelection.setData({"ORGANIZATIONALUNIT":"50006851","RELATIONSHIP":"B002"})
});
